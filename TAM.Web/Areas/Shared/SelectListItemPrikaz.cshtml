@{
    var Naslov = TempData["naslov"].ToString();
    var UrlUredi = TempData["urlUredi"].ToString();
    var UrlDodaj = TempData["urlDodaj"].ToString();
    var UrlObrisi = TempData["urlObrisi"].ToString();
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@model List<SelectListItem>

<table class="table table-hover">
    <thead class="card-header">
        <tr>
            <td>@Naslov</td>
            <td>Akcija</td>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Text</td>
                <td>
                    <button class="btn btn-info" onclick="AjaxForma(@item.Value, '@UrlUredi')">Uredi</button>
                    <button class="btn btn-danger"
                            onclick="location.href='@UrlObrisi?Id=@item.Value'">
                        Obrisi
                    </button>
                </td>
            </tr>
        }
    </tbody>
</table>

<button class="btn btn-dark" onclick="AjaxForma(0, '@UrlDodaj')">Dodaj</button>

<div id="forma"></div>

<script>
    function AjaxForma(Id, Url) {
        var url = Url + "?Id=" + Id;
        $.get(url, function (res) {
            $("#forma").html(res);
        });
    }
</script>